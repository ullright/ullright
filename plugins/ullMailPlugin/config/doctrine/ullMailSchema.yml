# Queue for batch mailing
UllMailQueuedMessage:
  package: ullMailPlugin.lib.model.doctrine
  inheritance:
    extends: UllRecord
    type: concrete
  columns:
    message:
      type: clob
      notnull: true
    is_sent:
      type: boolean
      default: false